## Available Scripts

In the project directory, you can run:

### `npm start`

Run this script inside the `blog-client` and the `blog-server` to start the application.
Executing this command inside those directories will start both client and server apps on ports 3000/3001.